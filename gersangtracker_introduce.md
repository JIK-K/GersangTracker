# 🗡️ GersangTracker — 발표 자료

> 거상(Gersang) 게임의 사냥 수익을 OCR로 자동 추적하는 데스크탑 애플리케이션

---

## 1. 프로젝트 소개

### 어떤 문제를 해결하는가?

거상은 국내 서비스 중인 MMORPG로, 플레이어가 몬스터를 사냥할 때마다 다양한 아이템이 드랍됩니다.
기존에는 이 드랍 기록을 **수동으로 직접 메모**하거나, 아예 기록하지 않는 경우가 대부분이었습니다.

> 💡 **"얼마나 벌었는지 자동으로 알려주는 도구가 있으면 어떨까?"**

GersangTracker는 이 불편함을 해소하기 위해 만든 **사냥 수익 자동 추적기**입니다.

### 핵심 가치

| | 기존 방식 | GersangTracker |
|---|---|---|
| 드랍 기록 | 수동 메모 | **OCR 자동 감지** |
| 수익 계산 | 직접 계산 | **자동 산출** |
| 데이터 보존 | 일회성 | **세션별 DB 저장** |
| 통계 확인 | 불가 | **세션 목록 + 엑셀 내보내기** |

---

## 2. 기술 스택

```
.NET 10  ·  WPF  ·  MVVM (CommunityToolkit.Mvvm)
SQLite (EF Core)  ·  OpenCvSharp4  ·  Tesseract OCR v5
EPPlus  ·  MahApps.Metro.IconPacks
```

### 선택 이유

- **WPF**: Windows 게임 환경 특성상 네이티브 데스크탑 앱이 적합. MVVM 패턴을 통한 관심사 분리
- **OpenCV + Tesseract**: 게임 화면을 직접 캡처하여 텍스트 추출. 설치 없이 로컬에서 완전히 동작
- **SQLite**: 별도 서버 없이 단일 파일로 영속 데이터 관리, 무결성 보장
- **Win32 API 직접 호출**: 게임 창만 선택적으로 캡처하여 CPU 부하 최소화

---

## 3. 시스템 아키텍처

### MVVM 구조

```
┌─────────────────────────────────────────────┐
│                   Views                      │
│  MainWindow · HuntingWindow · ResultWindow   │
│  PriceWindow · SessionWindow · Dialogs       │
└─────────────┬───────────────────────────────┘
              │  데이터 바인딩 / 커맨드
┌─────────────▼───────────────────────────────┐
│                 ViewModels                   │
│  MainViewModel · HuntingViewModel            │
│  PriceViewModel · ResultViewModel            │
└─────────────┬───────────────────────────────┘
              │  비즈니스 로직 호출
┌─────────────▼───────────────────────────────┐
│               Services (핵심 로직)           │
│  OcrService · DatabaseService · ExcelService │
└─────────────┬───────────────────────────────┘
              │  EF Core
┌─────────────▼───────────────────────────────┐
│            Models / SQLite DB                │
│  Monster · Session · DropLog · ItemPrice     │
└─────────────────────────────────────────────┘
```

### 주요 Service 역할

| Service | 역할 |
|---|---|
| `OcrService` | Win32 API로 게임 창 캡처 → OpenCV 전처리 → Tesseract OCR 인식 |
| `DatabaseService` | EF Core 기반 SQLite CRUD, 드랍 로그 병합(Merge) 최적화 |
| `ExcelService` | EPPlus로 세션 기록을 Excel 파일로 추출 |

---

## 4. 핵심 기능 흐름

### 사냥 세션 전체 흐름

```
몬스터 등록 (드랍 아이템 목록 설정)
        ↓
사냥 시작 → OCR 1초 주기 백그라운드 스캔
        ↓
드랍 메시지 감지 → 아이템명 + 수량 자동 기록
        ↓
사냥 종료 → 가격 입력 (단가 설정)
        ↓
결과 산출 (총수익 / 시간당 수익)
        ↓
세션 DB 저장 + 엑셀 내보내기
```

### 기능 목록

- **OCR 자동 드랍 감지** — 게임 창을 1초마다 캡처하여 드랍 메시지 자동 추출
- **오인식 자동 보정** — 레벤슈타인 거리 알고리즘으로 OCR 오타를 근접 아이템으로 매핑
- **수익 자동 계산** — 아이템별 단가 × 수량으로 총수익 및 시간당 수익(원/h) 산출
- **세션 관리** — 사냥 기록을 세션 단위로 저장, 히스토리 조회 가능
- **몬스터별 설정** — 몬스터마다 드랍 아이템 목록과 단가를 분리 관리
- **엑셀 내보내기** — EPPlus 기반 세션 결과 Excel 파일 추출

---

## 5. 기술적 도전 & 해결

### 도전 1 — 게임 화면 OCR 인식률 문제

**문제**
> 게임 내 드랍 메시지는 배경이 투명하고 폰트가 작아 Tesseract OCR이 정확히 인식하지 못함

**해결**

```
게임 창 전체 캡처
        ↓
드랍 메시지 영역(230×130px)만 정밀 크롭
        ↓
이미지 3배 확대 (고화질 보간)
        ↓
그레이스케일 변환 + 이진화(Threshold) 노이즈 제거
        ↓
Tesseract OCR 인식 → 인식률 대폭 향상
```

---

### 도전 2 — OCR 오타 매핑 문제

**문제**
> 전처리 후에도 `[장갑]` → `{장갑}`, `가죽` → `가줙` 같은 오타 발생

**해결 — 레벤슈타인 거리 알고리즘 적용**

```
OCR 인식 결과에서 한글만 정규식 추출
        ↓
DB에 등록된 아이템 목록과 레벤슈타인 거리 계산
        ↓
임계값 통과 시 (글자수 50% 또는 3자 이하 차이)
        ↓
가장 근접한 아이템으로 자동 매핑 (보정)
```

---

### 도전 3 — 중복 드랍 스캔 방지

**문제**
> 1초마다 캡처하므로 동일한 드랍 메시지가 여러 번 감지될 수 있음

**해결**
- 이전 캡처 줄(Line)과 현재 캡처 줄을 비교 → **신규 줄만 추출**
- `_lastConfirmedLines`와 교차 검증하여 **중복 카운트 원천 차단**

---

### 도전 4 — DB 로그 파편화 문제

**문제**
> 같은 아이템이 초 단위로 반복 기록되어 드랍 로그 행이 급증

**해결**
- `SyncDropLogsAsync` 로직: 동일 아이템의 다수 `DropLog`를 **단일 통합 로그로 병합(Merge)**
- DB 용량 절약 + 통계 쿼리 성능 최적화

---

## 6. 성과 & 회고

### 잘 된 점
- OCR 전처리 파이프라인 + 레벤슈타인 보정 조합으로 **노이즈 환경에서도 신뢰성 높은 실시간 텍스트 추출** 구현
- MVVM 패턴으로 View ↔ ViewModel 완전 분리, **유지보수성 높은 구조** 확보
- EF Core + SQLite로 **서버 없는 로컬 환경에서도 무결성 보장**

### 아쉬운 점 / 개선 여지
- Win32 API 의존으로 **Windows 전용** (macOS 미지원)
- OCR 인식 주기(1초)를 사용자가 조정할 수 없음
- 다중 게임 창 동시 지원 미구현

### 배운 점
- 저수준 Win32 API 직접 활용 경험 (화면 캡처, 창 핸들 탐색)
- OpenCV 이미지 전처리 파이프라인 설계
- WPF DataTemplate 내 애니메이션 트리거, MVVM 바인딩 심화

---

## 7. 링크

- **GitHub Releases**: [GersangTracker Releases](https://github.com/JIK-K/GersangTracker/releases)
- **Wiki**: [GersangTracker Wiki](https://github.com/JIK-K/GersangTracker/wiki)
