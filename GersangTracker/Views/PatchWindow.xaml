<Window x:Class="GersangTracker.Views.PatchWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewmodels="clr-namespace:GersangTracker.ViewModels"
        mc:Ignorable="d"
        Title="거상 패치 매니저" Height="250" Width="450" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">

    <Window.DataContext>
        <viewmodels:PatchViewModel/>
    </Window.DataContext>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding StatusText}" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10"/>

        <!-- 프로그레스 바 (오류를 일으키던 IsIndeterminate 속성 제거 완료) -->
        <ProgressBar Grid.Row="1" Value="{Binding ProgressValue}" Minimum="0" Maximum="100" Height="25" Margin="0,10,0,10" />

        <TextBlock Grid.Row="2" Text="{Binding DetailText}" FontSize="14" HorizontalAlignment="Center" Foreground="Gray" TextTrimming="CharacterEllipsis"/>

        <Button Grid.Row="4" Content="취소" Command="{Binding CancelCommand}" Width="100" Height="30" HorizontalAlignment="Center" Margin="0,15,0,0"/>
    </Grid>
</Window>